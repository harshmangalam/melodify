import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div>
      <div
        class=" absolute h-80 w-full"
        style={{ background: "rgb(72, 32, 176)" }}
      />
    </div>
  );
});
